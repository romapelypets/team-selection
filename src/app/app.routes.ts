import { Routes } from "@angular/router";
import { TeamSelectionContainerComponent } from "@team-selection/components/team-selection-container";

export const routes: Routes = [{ path: "", component: TeamSelectionContainerComponent }];
